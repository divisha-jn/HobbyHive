'use client';
import React from 'react'

const ListingCard = () => {
  return (
    <div>
      <button onClick={() => console.log("clicked")}>press me</button>
    </div>
  )
}

export default ListingCard;